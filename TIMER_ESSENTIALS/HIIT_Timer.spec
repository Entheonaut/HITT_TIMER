# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['hiit_timer.py'],
    pathex=[],
    binaries=[],
    datas=[('beep-warning.mp3', '.'), ('livechat.mp3', '.'), ('notification-positive.mp3', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='hiit_timer',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['hitt_time_icon.ico'],
)
